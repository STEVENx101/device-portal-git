spring:
  application:
    name: deviceportal

  profiles:
    active: dev
    
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp

server:
  port: 3478
  forward-headers-strategy: framework
  servlet:
    context-path: /device-portal

api:
  nimble:
    url: "https://api.nimble.example.com"
    username: "deviceportal_user"
    password: "deviceportal_password"